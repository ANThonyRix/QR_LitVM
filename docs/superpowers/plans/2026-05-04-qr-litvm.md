# QR LitVM — Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Web3 DApp на LitVM testnet — генерация QR-кодов и платёжных ссылок в zkLTC с on-chain запросами, username-short-links и embed-виджетом для сайтов.

**Architecture:** Next.js 15 фронт + wagmi v2/viem для Web3 + Solidity контракт на LitVM testnet (Liteforge). Все состояние хранится on-chain или в localStorage — бэкенд и БД не нужны. Embed-виджет — отдельная minimal-страница для iframe. Short links — через username registry в смарт-контракте.

**Tech Stack:** Next.js 15, TypeScript, Tailwind CSS, shadcn/ui, wagmi v2, viem, RainbowKit, qr-code-styling, Hardhat, Solidity ^0.8.24, Vercel (free)

---

## Перед началом: получить chain details LitVM

Перейди на https://builders.litvm.com или https://liteforge.hub.caldera.xyz и запиши:
- **Chain ID** (нужен для `defineChain`)
- **RPC HTTP URL** (нужен для транзакций)
- **Faucet URL** (нужен для получения тестовых zkLTC)
- **Explorer URL** (`https://liteforge.explorer.caldera.xyz`)

Везде ниже `LITVM_CHAIN_ID` и `LITVM_RPC_URL` — замени на реальные значения.

---

## Структура файлов

```
/
├── contracts/
│   ├── PaymentRequest.sol          # Единый контракт: запросы + username registry
│   └── test/
│       └── PaymentRequest.test.ts  # Hardhat тесты
├── scripts/
│   └── deploy.ts                   # Скрипт деплоя
├── src/
│   ├── app/
│   │   ├── layout.tsx              # Root layout + WagmiProvider
│   │   ├── page.tsx                # Главная: генератор платёжных ссылок
│   │   ├── pay/
│   │   │   └── [requestId]/
│   │   │       └── page.tsx        # Страница оплаты (on-chain запрос)
│   │   ├── u/
│   │   │   └── [username]/
│   │   │       └── page.tsx        # Профиль пользователя (short link)
│   │   └── widget/
│   │       └── [requestId]/
│   │           └── page.tsx        # Embed-виджет (iframe)
│   ├── components/
│   │   ├── WalletConnect.tsx       # RainbowKit ConnectButton
│   │   ├── PaymentGenerator.tsx    # Форма создания on-chain запроса
│   │   ├── QRDisplay.tsx           # QR с логотипом + скачать PNG/SVG
│   │   ├── PayButton.tsx           # Кнопка «Оплатить» (useSendTransaction)
│   │   ├── PaymentStatus.tsx       # Статус оплаты (paid/pending)
│   │   ├── UsernameRegister.tsx    # Форма регистрации username
│   │   ├── UserProfile.tsx         # Профиль /u/[username]
│   │   └── EmbedCode.tsx           # Сниппет <iframe> кода
│   ├── hooks/
│   │   ├── usePaymentRequest.ts    # Читает запрос из контракта
│   │   ├── useCreateRequest.ts     # Создаёт запрос в контракте
│   │   ├── usePay.ts               # Отправляет оплату
│   │   └── useUsername.ts          # Регистрация/чтение username
│   └── lib/
│       ├── chain.ts                # defineChain для LitVM
│       ├── wagmi.ts                # createConfig
│       └── contract.ts             # ABI + адрес контракта
├── hardhat.config.ts
├── .env.local                      # LITVM_RPC_URL, CONTRACT_ADDRESS, PRIVATE_KEY
└── next.config.ts
```

---

## Chunk 1: Project Setup + Smart Contract

### Task 1: Scaffolding проекта

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `tailwind.config.ts`
- Create: `hardhat.config.ts`
- Create: `.env.local`

- [ ] **Step 1: Инициализировать Next.js проект**

```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
```

- [ ] **Step 2: Установить Web3 зависимости**

```bash
npm install wagmi viem @rainbow-me/rainbowkit @tanstack/react-query
```

- [ ] **Step 3: Установить UI и QR зависимости**

```bash
npm install qr-code-styling
npx shadcn@latest init
npx shadcn@latest add button input label card badge separator tabs
```

- [ ] **Step 4: Установить Hardhat и Solidity зависимости**

```bash
npm install --save-dev hardhat @nomicfoundation/hardhat-toolbox @types/node
npx hardhat init
# Выбрать: TypeScript project
```

- [ ] **Step 5: Создать `.env.local`**

```bash
# .env.local
LITVM_RPC_URL=https://YOUR_LITVM_RPC_URL
NEXT_PUBLIC_LITVM_CHAIN_ID=YOUR_CHAIN_ID
NEXT_PUBLIC_CONTRACT_ADDRESS=     # заполнить после деплоя
DEPLOYER_PRIVATE_KEY=             # кошелёк для деплоя (только в .gitignore!)
```

- [ ] **Step 6: Добавить `.env.local` в `.gitignore`**

```
echo ".env.local" >> .gitignore
echo "DEPLOYER_PRIVATE_KEY" >> .gitignore
```

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "chore: initial project scaffold"
```

---

### Task 2: LitVM chain config + wagmi setup

**Files:**
- Create: `src/lib/chain.ts`
- Create: `src/lib/wagmi.ts`

- [ ] **Step 1: Написать `src/lib/chain.ts`**

```ts
import { defineChain } from 'viem'

export const litvm = defineChain({
  id: Number(process.env.NEXT_PUBLIC_LITVM_CHAIN_ID),
  name: 'LitVM Liteforge Testnet',
  nativeCurrency: {
    name: 'zkLTC',
    symbol: 'zkLTC',
    decimals: 18,
  },
  rpcUrls: {
    default: { http: [process.env.LITVM_RPC_URL ?? ''] },
  },
  blockExplorers: {
    default: {
      name: 'Liteforge Explorer',
      url: 'https://liteforge.explorer.caldera.xyz',
    },
  },
  testnet: true,
})
```

- [ ] **Step 2: Написать `src/lib/wagmi.ts`**

```ts
import { createConfig, http } from 'wagmi'
import { litvm } from './chain'

export const config = createConfig({
  chains: [litvm],
  transports: {
    [litvm.id]: http(),
  },
  ssr: true,
})
```

- [ ] **Step 3: Обновить `src/app/layout.tsx` — добавить провайдеры**

```tsx
'use client'
import { WagmiProvider } from 'wagmi'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { RainbowKitProvider } from '@rainbow-me/rainbowkit'
import { config } from '@/lib/wagmi'
import '@rainbow-me/rainbowkit/styles.css'

const queryClient = new QueryClient()

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ru">
      <body>
        <WagmiProvider config={config}>
          <QueryClientProvider client={queryClient}>
            <RainbowKitProvider>
              {children}
            </RainbowKitProvider>
          </QueryClientProvider>
        </WagmiProvider>
      </body>
    </html>
  )
}
```

- [ ] **Step 4: Проверить — запустить dev**

```bash
npm run dev
# Ожидаем: сервер стартует без ошибок на localhost:3000
```

- [ ] **Step 5: Commit**

```bash
git add src/lib/chain.ts src/lib/wagmi.ts src/app/layout.tsx
git commit -m "feat: add LitVM chain config and wagmi provider"
```

---

### Task 3: Смарт-контракт PaymentRequest.sol

**Files:**
- Create: `contracts/PaymentRequest.sol`

- [ ] **Step 1: Написать контракт**

```solidity
// contracts/PaymentRequest.sol
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.24;

contract PaymentRequest {
    struct Request {
        address payable recipient;
        uint256 amount;   // 0 = любая сумма
        string  label;
        bool    paid;
        address payer;
        uint256 paidAt;
    }

    // Payment requests
    mapping(bytes32 => Request) public requests;

    // Username registry
    mapping(string  => address) public usernameToAddress;
    mapping(address => string)  public addressToUsername;

    event RequestCreated(
        bytes32 indexed id,
        address indexed recipient,
        uint256 amount,
        string  label
    );
    event RequestPaid(
        bytes32 indexed id,
        address indexed payer,
        uint256 amount
    );
    event UsernameRegistered(
        address indexed user,
        string username
    );

    // --- Payment Requests ---

    function createRequest(
        uint256 amount,
        string calldata label
    ) external returns (bytes32 id) {
        id = keccak256(
            abi.encodePacked(msg.sender, amount, label, block.timestamp, block.prevrandao)
        );
        requests[id] = Request({
            recipient: payable(msg.sender),
            amount:    amount,
            label:     label,
            paid:      false,
            payer:     address(0),
            paidAt:    0
        });
        emit RequestCreated(id, msg.sender, amount, label);
    }

    function pay(bytes32 id) external payable {
        Request storage req = requests[id];
        require(req.recipient != address(0), "Not found");
        require(!req.paid, "Already paid");
        if (req.amount > 0) {
            require(msg.value == req.amount, "Wrong amount");
        } else {
            require(msg.value > 0, "Send zkLTC");
        }

        req.paid   = true;
        req.payer  = msg.sender;
        req.paidAt = block.timestamp;

        (bool ok,) = req.recipient.call{value: msg.value}("");
        require(ok, "Transfer failed");

        emit RequestPaid(id, msg.sender, msg.value);
    }

    // --- Username Registry ---

    function registerUsername(string calldata username) external {
        require(bytes(username).length >= 3,  "Min 3 chars");
        require(bytes(username).length <= 32, "Max 32 chars");
        require(usernameToAddress[username] == address(0), "Taken");
        require(bytes(addressToUsername[msg.sender]).length == 0, "Already registered");

        usernameToAddress[username] = msg.sender;
        addressToUsername[msg.sender] = username;

        emit UsernameRegistered(msg.sender, username);
    }
}
```

- [ ] **Step 2: Commit**

```bash
git add contracts/PaymentRequest.sol
git commit -m "feat: add PaymentRequest contract with username registry"
```

---

### Task 4: Тесты контракта

**Files:**
- Create: `contracts/test/PaymentRequest.test.ts`

- [ ] **Step 1: Написать тесты**

```ts
// contracts/test/PaymentRequest.test.ts
import { expect } from 'chai'
import { ethers } from 'hardhat'

describe('PaymentRequest', () => {
  async function deploy() {
    const [owner, payer, other] = await ethers.getSigners()
    const Contract = await ethers.getContractFactory('PaymentRequest')
    const contract = await Contract.deploy()
    return { contract, owner, payer, other }
  }

  describe('createRequest', () => {
    it('creates a request and emits event', async () => {
      const { contract, owner } = await deploy()
      const amount = ethers.parseEther('1')
      const tx = await contract.createRequest(amount, 'Freelance job')
      const receipt = await tx.wait()
      const event = receipt?.logs[0]
      expect(event).to.exist
    })

    it('creates open-amount request when amount=0', async () => {
      const { contract } = await deploy()
      await expect(contract.createRequest(0, 'Donation')).to.emit(contract, 'RequestCreated')
    })
  })

  describe('pay', () => {
    it('pays exact amount and marks as paid', async () => {
      const { contract, payer } = await deploy()
      const amount = ethers.parseEther('0.1')
      const tx = await contract.createRequest(amount, 'Test')
      const receipt = await tx.wait()
      const id = (receipt?.logs[0] as any).topics[1]

      await expect(
        contract.connect(payer).pay(id, { value: amount })
      ).to.emit(contract, 'RequestPaid')

      const req = await contract.requests(id)
      expect(req.paid).to.be.true
      expect(req.payer).to.equal(payer.address)
    })

    it('reverts if wrong amount', async () => {
      const { contract, payer } = await deploy()
      const amount = ethers.parseEther('1')
      const tx = await contract.createRequest(amount, 'Test')
      const receipt = await tx.wait()
      const id = (receipt?.logs[0] as any).topics[1]

      await expect(
        contract.connect(payer).pay(id, { value: ethers.parseEther('0.5') })
      ).to.be.revertedWith('Wrong amount')
    })

    it('reverts on double payment', async () => {
      const { contract, payer } = await deploy()
      const amount = ethers.parseEther('0.1')
      const tx = await contract.createRequest(amount, 'Test')
      const receipt = await tx.wait()
      const id = (receipt?.logs[0] as any).topics[1]

      await contract.connect(payer).pay(id, { value: amount })
      await expect(
        contract.connect(payer).pay(id, { value: amount })
      ).to.be.revertedWith('Already paid')
    })
  })

  describe('registerUsername', () => {
    it('registers a username', async () => {
      const { contract, owner } = await deploy()
      await expect(contract.registerUsername('alice'))
        .to.emit(contract, 'UsernameRegistered')
        .withArgs(owner.address, 'alice')
      expect(await contract.usernameToAddress('alice')).to.equal(owner.address)
    })

    it('reverts on duplicate username', async () => {
      const { contract, owner, other } = await deploy()
      await contract.registerUsername('alice')
      await expect(
        contract.connect(other).registerUsername('alice')
      ).to.be.revertedWith('Taken')
    })

    it('reverts if address already registered', async () => {
      const { contract, owner } = await deploy()
      await contract.registerUsername('alice')
      await expect(contract.registerUsername('bob')).to.be.revertedWith('Already registered')
    })

    it('rejects username shorter than 3 chars', async () => {
      const { contract } = await deploy()
      await expect(contract.registerUsername('ab')).to.be.revertedWith('Min 3 chars')
    })
  })
})
```

- [ ] **Step 2: Запустить тесты — убедиться что проходят**

```bash
npx hardhat test
# Ожидаем: 8 passing
```

- [ ] **Step 3: Commit**

```bash
git add contracts/test/PaymentRequest.test.ts
git commit -m "test: add PaymentRequest contract tests"
```

---

### Task 5: Деплой контракта на LitVM testnet

**Files:**
- Create: `scripts/deploy.ts`
- Modify: `hardhat.config.ts`

- [ ] **Step 1: Обновить `hardhat.config.ts`**

```ts
import { HardhatUserConfig } from 'hardhat/config'
import '@nomicfoundation/hardhat-toolbox'
import * as dotenv from 'dotenv'
dotenv.config({ path: '.env.local' })

const config: HardhatUserConfig = {
  solidity: '0.8.24',
  networks: {
    litvm: {
      url: process.env.LITVM_RPC_URL ?? '',
      accounts: process.env.DEPLOYER_PRIVATE_KEY
        ? [process.env.DEPLOYER_PRIVATE_KEY]
        : [],
      chainId: Number(process.env.NEXT_PUBLIC_LITVM_CHAIN_ID),
    },
  },
}
export default config
```

- [ ] **Step 2: Написать `scripts/deploy.ts`**

```ts
import { ethers } from 'hardhat'

async function main() {
  const [deployer] = await ethers.getSigners()
  console.log('Deploying from:', deployer.address)

  const Contract = await ethers.getContractFactory('PaymentRequest')
  const contract = await Contract.deploy()
  await contract.waitForDeployment()

  const address = await contract.getAddress()
  console.log('PaymentRequest deployed to:', address)
  console.log('Add to .env.local: NEXT_PUBLIC_CONTRACT_ADDRESS=' + address)
}

main().catch((e) => { console.error(e); process.exit(1) })
```

- [ ] **Step 3: Получить тестовые zkLTC из faucet**

Перейти на faucet (URL из builders.litvm.com), получить zkLTC на адрес DEPLOYER кошелька.

- [ ] **Step 4: Задеплоить**

```bash
npx hardhat run scripts/deploy.ts --network litvm
# Ожидаем: адрес контракта в выводе
# Скопировать адрес в .env.local → NEXT_PUBLIC_CONTRACT_ADDRESS=0x...
```

- [ ] **Step 5: Commit**

```bash
git add hardhat.config.ts scripts/deploy.ts
git commit -m "feat: add deploy script for LitVM testnet"
```

---

## Chunk 2: Core DApp — Генератор + Страница оплаты

### Task 6: ABI + contract lib + хуки

**Files:**
- Create: `src/lib/contract.ts`
- Create: `src/hooks/useCreateRequest.ts`
- Create: `src/hooks/usePaymentRequest.ts`
- Create: `src/hooks/usePay.ts`

- [ ] **Step 1: После деплоя скопировать ABI**

```bash
# ABI генерируется Hardhat в artifacts/
cp artifacts/contracts/PaymentRequest.sol/PaymentRequest.json src/lib/PaymentRequest.json
```

- [ ] **Step 2: Написать `src/lib/contract.ts`**

```ts
import abi from './PaymentRequest.json'

export const CONTRACT_ADDRESS =
  (process.env.NEXT_PUBLIC_CONTRACT_ADDRESS ?? '') as `0x${string}`

export const CONTRACT_ABI = abi.abi
```

- [ ] **Step 3: Написать `src/hooks/useCreateRequest.ts`**

```ts
import { useWriteContract, useWaitForTransactionReceipt } from 'wagmi'
import { CONTRACT_ADDRESS, CONTRACT_ABI } from '@/lib/contract'
import { parseEther } from 'viem'

export function useCreateRequest() {
  const { writeContract, data: hash, isPending, error } = useWriteContract()

  function create(amountEth: string, label: string) {
    const amount = amountEth ? parseEther(amountEth) : 0n
    writeContract({
      address: CONTRACT_ADDRESS,
      abi: CONTRACT_ABI,
      functionName: 'createRequest',
      args: [amount, label],
    })
  }

  const { isLoading: isConfirming, data: receipt } =
    useWaitForTransactionReceipt({ hash })

  // Extract requestId from RequestCreated event log
  const requestId = receipt?.logs?.[0]?.topics?.[1]

  return { create, hash, isPending, isConfirming, requestId, error }
}
```

- [ ] **Step 4: Написать `src/hooks/usePaymentRequest.ts`**

```ts
import { useReadContract } from 'wagmi'
import { CONTRACT_ADDRESS, CONTRACT_ABI } from '@/lib/contract'

export function usePaymentRequest(id: `0x${string}` | undefined) {
  const { data, isLoading, error } = useReadContract({
    address: CONTRACT_ADDRESS,
    abi: CONTRACT_ABI,
    functionName: 'requests',
    args: id ? [id] : undefined,
    query: { enabled: !!id },
  })

  return { request: data as any, isLoading, error }
}
```

- [ ] **Step 5: Написать `src/hooks/usePay.ts`**

```ts
import { useWriteContract, useWaitForTransactionReceipt } from 'wagmi'
import { CONTRACT_ADDRESS, CONTRACT_ABI } from '@/lib/contract'
import { parseEther } from 'viem'

export function usePay() {
  const { writeContract, data: hash, isPending, error } = useWriteContract()

  function pay(requestId: `0x${string}`, amountEth: string) {
    writeContract({
      address: CONTRACT_ADDRESS,
      abi: CONTRACT_ABI,
      functionName: 'pay',
      args: [requestId],
      value: parseEther(amountEth),
    })
  }

  const { isLoading: isConfirming, isSuccess } =
    useWaitForTransactionReceipt({ hash })

  return { pay, hash, isPending, isConfirming, isSuccess, error }
}
```

- [ ] **Step 6: Commit**

```bash
git add src/lib/contract.ts src/hooks/
git commit -m "feat: add contract hooks (create, read, pay)"
```

---

### Task 7: WalletConnect компонент

**Files:**
- Create: `src/components/WalletConnect.tsx`

- [ ] **Step 1: Написать компонент**

```tsx
// src/components/WalletConnect.tsx
'use client'
import { ConnectButton } from '@rainbow-me/rainbowkit'

export function WalletConnect() {
  return (
    <ConnectButton
      showBalance={{ smallScreen: false, largeScreen: true }}
      chainStatus="icon"
    />
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/WalletConnect.tsx
git commit -m "feat: add WalletConnect component"
```

---

### Task 8: QRDisplay компонент

**Files:**
- Create: `src/components/QRDisplay.tsx`

- [ ] **Step 1: Написать компонент**

```tsx
// src/components/QRDisplay.tsx
'use client'
import { useEffect, useRef } from 'react'
import QRCodeStyling from 'qr-code-styling'
import { Button } from '@/components/ui/button'

interface QRDisplayProps {
  url: string
  label?: string
}

export function QRDisplay({ url, label }: QRDisplayProps) {
  const ref = useRef<HTMLDivElement>(null)
  const qrRef = useRef<QRCodeStyling | null>(null)

  useEffect(() => {
    if (!qrRef.current) {
      qrRef.current = new QRCodeStyling({
        width: 280,
        height: 280,
        type: 'svg',
        data: url,
        image: '/logo.png',   // положить логотип в public/logo.png
        dotsOptions: { color: '#1a1a2e', type: 'rounded' },
        cornersSquareOptions: { type: 'extra-rounded', color: '#1a1a2e' },
        backgroundOptions: { color: '#ffffff' },
        imageOptions: {
          imageSize: 0.35,
          margin: 8,
          hideBackgroundDots: true,
          crossOrigin: 'anonymous',
        },
        qrOptions: { errorCorrectionLevel: 'H' },
      })
    }
    if (ref.current) {
      ref.current.innerHTML = ''
      qrRef.current.append(ref.current)
    }
  }, [])

  useEffect(() => {
    qrRef.current?.update({ data: url })
  }, [url])

  const downloadSVG = () => qrRef.current?.download({ name: label ?? 'qr-litvm', extension: 'svg' })
  const downloadPNG = () => qrRef.current?.download({ name: label ?? 'qr-litvm', extension: 'png' })

  return (
    <div className="flex flex-col items-center gap-3">
      <div ref={ref} className="rounded-xl overflow-hidden shadow-lg" />
      <div className="flex gap-2">
        <Button variant="outline" size="sm" onClick={downloadSVG}>SVG</Button>
        <Button variant="outline" size="sm" onClick={downloadPNG}>PNG</Button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Добавить логотип**

Положить `public/logo.png` — логотип QR LitVM (минимальный, 100×100px).

- [ ] **Step 3: Commit**

```bash
git add src/components/QRDisplay.tsx public/logo.png
git commit -m "feat: add QRDisplay component with download"
```

---

### Task 9: PaymentGenerator — форма создания запроса

**Files:**
- Create: `src/components/PaymentGenerator.tsx`
- Modify: `src/app/page.tsx`

- [ ] **Step 1: Написать `src/components/PaymentGenerator.tsx`**

```tsx
// src/components/PaymentGenerator.tsx
'use client'
import { useState, useEffect } from 'react'
import { useAccount } from 'wagmi'
import { useCreateRequest } from '@/hooks/useCreateRequest'
import { QRDisplay } from './QRDisplay'
import { EmbedCode } from './EmbedCode'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'

export function PaymentGenerator() {
  const { isConnected } = useAccount()
  const [amount, setAmount]   = useState('')
  const [label, setLabel]     = useState('')
  const [payUrl, setPayUrl]   = useState('')
  const [copied, setCopied]   = useState(false)

  const { create, isPending, isConfirming, requestId, error } = useCreateRequest()

  useEffect(() => {
    if (requestId) {
      const url = `${window.location.origin}/pay/${requestId}`
      setPayUrl(url)
      // Сохранить в историю localStorage
      const history = JSON.parse(localStorage.getItem('qrlitvm_history') ?? '[]')
      history.unshift({ id: requestId, url, label, amount, createdAt: Date.now() })
      localStorage.setItem('qrlitvm_history', JSON.stringify(history.slice(0, 50)))
    }
  }, [requestId])

  const handleCreate = () => {
    if (!label.trim()) return
    create(amount, label)
  }

  const copyLink = async () => {
    await navigator.clipboard.writeText(payUrl)
    setCopied(true)
    setTimeout(() => setCopied(false), 2000)
  }

  if (!isConnected) {
    return (
      <Card>
        <CardContent className="pt-6 text-center text-muted-foreground">
          Подключи кошелёк для создания платёжной ссылки
        </CardContent>
      </Card>
    )
  }

  return (
    <div className="space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>Создать платёжную ссылку</CardTitle>
        </CardHeader>
        <CardContent className="space-y-4">
          <div className="space-y-1">
            <Label>Описание *</Label>
            <Input
              placeholder="Фриланс проект / Товар"
              value={label}
              onChange={e => setLabel(e.target.value)}
            />
          </div>
          <div className="space-y-1">
            <Label>Сумма zkLTC (оставь пустым — любая сумма)</Label>
            <Input
              type="number"
              placeholder="0.5"
              value={amount}
              onChange={e => setAmount(e.target.value)}
              min="0"
              step="0.001"
            />
          </div>
          <Button
            className="w-full"
            onClick={handleCreate}
            disabled={!label.trim() || isPending || isConfirming}
          >
            {isPending ? 'Подтверди в кошельке...' :
             isConfirming ? 'Ждём блокчейн...' :
             'Создать ссылку'}
          </Button>
          {error && (
            <p className="text-sm text-destructive">{error.message}</p>
          )}
        </CardContent>
      </Card>

      {payUrl && (
        <Card>
          <CardContent className="pt-6">
            <Tabs defaultValue="qr">
              <TabsList className="w-full">
                <TabsTrigger value="qr" className="flex-1">QR-код</TabsTrigger>
                <TabsTrigger value="link" className="flex-1">Ссылка</TabsTrigger>
                <TabsTrigger value="embed" className="flex-1">Виджет</TabsTrigger>
              </TabsList>
              <TabsContent value="qr" className="flex justify-center py-4">
                <QRDisplay url={payUrl} label={label} />
              </TabsContent>
              <TabsContent value="link" className="space-y-3 py-4">
                <div className="bg-muted rounded-lg p-3 break-all text-sm font-mono">
                  {payUrl}
                </div>
                <Button variant="outline" className="w-full" onClick={copyLink}>
                  {copied ? 'Скопировано!' : 'Копировать ссылку'}
                </Button>
              </TabsContent>
              <TabsContent value="embed" className="py-4">
                <EmbedCode requestId={requestId!} />
              </TabsContent>
            </Tabs>
          </CardContent>
        </Card>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Обновить `src/app/page.tsx`**

```tsx
import { WalletConnect } from '@/components/WalletConnect'
import { PaymentGenerator } from '@/components/PaymentGenerator'

export default function Home() {
  return (
    <main className="min-h-screen bg-gradient-to-br from-slate-900 to-slate-800">
      <div className="max-w-xl mx-auto px-4 py-8 space-y-6">
        <header className="flex items-center justify-between">
          <div>
            <h1 className="text-2xl font-bold text-white">QR LitVM</h1>
            <p className="text-slate-400 text-sm">Платёжные ссылки в zkLTC</p>
          </div>
          <WalletConnect />
        </header>
        <PaymentGenerator />
      </div>
    </main>
  )
}
```

- [ ] **Step 3: Проверить вручную**

```bash
npm run dev
# 1. Открыть localhost:3000
# 2. Подключить кошелёк (MetaMask, выбрать LitVM testnet)
# 3. Ввести описание + сумму → Create
# 4. Проверить: появился URL и QR
```

- [ ] **Step 4: Commit**

```bash
git add src/components/PaymentGenerator.tsx src/app/page.tsx
git commit -m "feat: payment generator with QR, link, and embed tabs"
```

---

### Task 10: Страница оплаты `/pay/[requestId]`

**Files:**
- Create: `src/components/PayButton.tsx`
- Create: `src/components/PaymentStatus.tsx`
- Create: `src/app/pay/[requestId]/page.tsx`

- [ ] **Step 1: Написать `src/components/PaymentStatus.tsx`**

```tsx
'use client'
import { Badge } from '@/components/ui/badge'

interface Props {
  paid: boolean
  payer?: string
}

export function PaymentStatus({ paid, payer }: Props) {
  return paid ? (
    <div className="space-y-1">
      <Badge className="bg-green-500">Оплачено</Badge>
      {payer && (
        <p className="text-xs text-muted-foreground font-mono">
          от {payer.slice(0, 6)}...{payer.slice(-4)}
        </p>
      )}
    </div>
  ) : (
    <Badge variant="outline">Ожидает оплаты</Badge>
  )
}
```

- [ ] **Step 2: Написать `src/components/PayButton.tsx`**

```tsx
'use client'
import { useState } from 'react'
import { useAccount } from 'wagmi'
import { usePay } from '@/hooks/usePay'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { formatEther } from 'viem'

interface Props {
  requestId: `0x${string}`
  fixedAmount: bigint    // 0n = открытая сумма
  paid: boolean
}

export function PayButton({ requestId, fixedAmount, paid }: Props) {
  const { isConnected } = useAccount()
  const { pay, isPending, isConfirming, isSuccess, error } = usePay()
  const [customAmount, setCustomAmount] = useState('')

  if (paid || isSuccess) {
    return <p className="text-green-600 font-medium">Оплата подтверждена!</p>
  }

  if (!isConnected) {
    return <p className="text-sm text-muted-foreground">Подключи кошелёк для оплаты</p>
  }

  const amount = fixedAmount > 0n
    ? formatEther(fixedAmount)
    : customAmount

  return (
    <div className="space-y-3">
      {fixedAmount === 0n && (
        <Input
          type="number"
          placeholder="Введи сумму zkLTC"
          value={customAmount}
          onChange={e => setCustomAmount(e.target.value)}
          min="0.001"
          step="0.001"
        />
      )}
      <Button
        className="w-full"
        size="lg"
        disabled={!amount || isPending || isConfirming}
        onClick={() => pay(requestId, amount)}
      >
        {isPending ? 'Подтверди в кошельке...' :
         isConfirming ? 'Транзакция...' :
         `Оплатить ${amount} zkLTC`}
      </Button>
      {error && <p className="text-sm text-destructive">{error.message}</p>}
    </div>
  )
}
```

- [ ] **Step 3: Написать `src/app/pay/[requestId]/page.tsx`**

```tsx
// src/app/pay/[requestId]/page.tsx
'use client'
import { use } from 'react'
import { usePaymentRequest } from '@/hooks/usePaymentRequest'
import { WalletConnect } from '@/components/WalletConnect'
import { PayButton } from '@/components/PayButton'
import { PaymentStatus } from '@/components/PaymentStatus'
import { QRDisplay } from '@/components/QRDisplay'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { formatEther } from 'viem'

export default function PayPage({
  params,
}: {
  params: Promise<{ requestId: string }>
}) {
  const { requestId } = use(params)
  const id = requestId as `0x${string}`
  const { request, isLoading } = usePaymentRequest(id)

  const pageUrl = typeof window !== 'undefined'
    ? window.location.href
    : ''

  if (isLoading) {
    return (
      <main className="min-h-screen flex items-center justify-center bg-slate-900">
        <p className="text-slate-400">Загрузка...</p>
      </main>
    )
  }

  if (!request?.recipient || request.recipient === '0x0000000000000000000000000000000000000000') {
    return (
      <main className="min-h-screen flex items-center justify-center bg-slate-900">
        <Card className="max-w-sm w-full mx-4">
          <CardContent className="pt-6 text-center text-muted-foreground">
            Платёжный запрос не найден
          </CardContent>
        </Card>
      </main>
    )
  }

  const amount: bigint = request.amount ?? 0n
  const amountDisplay = amount > 0n ? `${formatEther(amount)} zkLTC` : 'Любая сумма'

  return (
    <main className="min-h-screen bg-gradient-to-br from-slate-900 to-slate-800">
      <div className="max-w-md mx-auto px-4 py-8 space-y-6">
        <header className="flex items-center justify-between">
          <h1 className="text-xl font-bold text-white">QR LitVM</h1>
          <WalletConnect />
        </header>

        <Card>
          <CardHeader>
            <CardTitle>{request.label}</CardTitle>
          </CardHeader>
          <CardContent className="space-y-4">
            <div className="flex items-center justify-between">
              <span className="text-2xl font-bold">{amountDisplay}</span>
              <PaymentStatus paid={request.paid} payer={request.payer} />
            </div>
            <p className="text-xs text-muted-foreground font-mono">
              Получатель: {request.recipient}
            </p>
            {!request.paid && (
              <PayButton
                requestId={id}
                fixedAmount={amount}
                paid={request.paid}
              />
            )}
          </CardContent>
        </Card>

        <div className="flex justify-center">
          <QRDisplay url={pageUrl} label={request.label} />
        </div>
      </div>
    </main>
  )
}
```

- [ ] **Step 4: Проверить вручную**

```bash
# 1. Создать запрос на главной → скопировать URL
# 2. Открыть /pay/[requestId] в другом браузере/кошельке
# 3. Оплатить → убедиться что статус меняется на "Оплачено"
```

- [ ] **Step 5: Commit**

```bash
git add src/components/PayButton.tsx src/components/PaymentStatus.tsx src/app/pay/
git commit -m "feat: payment page with on-chain status and pay button"
```

---

## Chunk 3: Short Links + Embed Widget

### Task 11: Username hook + компонент регистрации

**Files:**
- Create: `src/hooks/useUsername.ts`
- Create: `src/components/UsernameRegister.tsx`

- [ ] **Step 1: Написать `src/hooks/useUsername.ts`**

```ts
import { useReadContract, useWriteContract, useWaitForTransactionReceipt, useAccount } from 'wagmi'
import { CONTRACT_ADDRESS, CONTRACT_ABI } from '@/lib/contract'

export function useUsername() {
  const { address } = useAccount()

  // Читаем username текущего пользователя
  const { data: myUsername, refetch } = useReadContract({
    address: CONTRACT_ADDRESS,
    abi: CONTRACT_ABI,
    functionName: 'addressToUsername',
    args: address ? [address] : undefined,
    query: { enabled: !!address },
  })

  // Читаем адрес по username (для /u/[username])
  const useAddressForUsername = (username: string) =>
    useReadContract({
      address: CONTRACT_ADDRESS,
      abi: CONTRACT_ABI,
      functionName: 'usernameToAddress',
      args: [username],
      query: { enabled: !!username },
    })

  const { writeContract, data: hash, isPending, error } = useWriteContract()

  const register = (username: string) => {
    writeContract({
      address: CONTRACT_ADDRESS,
      abi: CONTRACT_ABI,
      functionName: 'registerUsername',
      args: [username],
    })
  }

  const { isLoading: isConfirming, isSuccess } =
    useWaitForTransactionReceipt({ hash })

  return {
    myUsername: myUsername as string | undefined,
    useAddressForUsername,
    register,
    isPending,
    isConfirming,
    isSuccess,
    error,
    refetch,
  }
}
```

- [ ] **Step 2: Написать `src/components/UsernameRegister.tsx`**

```tsx
'use client'
import { useState, useEffect } from 'react'
import { useUsername } from '@/hooks/useUsername'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'

export function UsernameRegister() {
  const [input, setInput] = useState('')
  const { myUsername, register, isPending, isConfirming, isSuccess, error, refetch } = useUsername()

  useEffect(() => { if (isSuccess) refetch() }, [isSuccess])

  if (myUsername) {
    return (
      <Card>
        <CardContent className="pt-4 flex items-center gap-3">
          <span className="text-sm text-muted-foreground">Твоя короткая ссылка:</span>
          <Badge variant="secondary">
            {typeof window !== 'undefined' ? window.location.origin : ''}/u/{myUsername}
          </Badge>
        </CardContent>
      </Card>
    )
  }

  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-base">Получить короткую ссылку</CardTitle>
      </CardHeader>
      <CardContent className="space-y-3">
        <div className="space-y-1">
          <Label>Username (мин. 3 символа)</Label>
          <Input
            placeholder="alice"
            value={input}
            onChange={e => setInput(e.target.value.toLowerCase().replace(/[^a-z0-9_]/g, ''))}
            maxLength={32}
          />
        </div>
        <Button
          className="w-full"
          onClick={() => register(input)}
          disabled={input.length < 3 || isPending || isConfirming}
        >
          {isPending ? 'Подтверди...' : isConfirming ? 'Ждём...' : 'Зарегистрировать'}
        </Button>
        {error && <p className="text-sm text-destructive">{error.message}</p>}
      </CardContent>
    </Card>
  )
}
```

- [ ] **Step 3: Добавить UsernameRegister на главную страницу**

В `src/app/page.tsx` после `<PaymentGenerator />`:

```tsx
import { UsernameRegister } from '@/components/UsernameRegister'
// ...
<UsernameRegister />
```

- [ ] **Step 4: Commit**

```bash
git add src/hooks/useUsername.ts src/components/UsernameRegister.tsx src/app/page.tsx
git commit -m "feat: username registry with short link display"
```

---

### Task 12: Профиль `/u/[username]`

**Files:**
- Create: `src/components/UserProfile.tsx`
- Create: `src/app/u/[username]/page.tsx`

- [ ] **Step 1: Написать `src/app/u/[username]/page.tsx`**

```tsx
'use client'
import { use } from 'react'
import { useReadContract } from 'wagmi'
import { CONTRACT_ADDRESS, CONTRACT_ABI } from '@/lib/contract'
import { WalletConnect } from '@/components/WalletConnect'
import { PaymentGenerator } from '@/components/PaymentGenerator'
import { Card, CardContent } from '@/components/ui/card'

export default function UserProfilePage({
  params,
}: {
  params: Promise<{ username: string }>
}) {
  const { username } = use(params)

  const { data: address, isLoading } = useReadContract({
    address: CONTRACT_ADDRESS,
    abi: CONTRACT_ABI,
    functionName: 'usernameToAddress',
    args: [username],
  })

  if (isLoading) {
    return (
      <main className="min-h-screen flex items-center justify-center bg-slate-900">
        <p className="text-slate-400">Загрузка...</p>
      </main>
    )
  }

  // Адрес не найден = username не зарегистрирован
  if (!address || address === '0x0000000000000000000000000000000000000000') {
    return (
      <main className="min-h-screen flex items-center justify-center bg-slate-900">
        <Card className="max-w-sm w-full mx-4">
          <CardContent className="pt-6 text-center text-muted-foreground">
            Пользователь <strong>@{username}</strong> не найден
          </CardContent>
        </Card>
      </main>
    )
  }

  // Если это наш кошелёк — показать генератор прямо здесь
  return (
    <main className="min-h-screen bg-gradient-to-br from-slate-900 to-slate-800">
      <div className="max-w-xl mx-auto px-4 py-8 space-y-6">
        <header className="flex items-center justify-between">
          <div>
            <h1 className="text-2xl font-bold text-white">@{username}</h1>
            <p className="text-slate-400 text-xs font-mono">
              {(address as string).slice(0, 8)}...{(address as string).slice(-6)}
            </p>
          </div>
          <WalletConnect />
        </header>

        {/* Быстрая форма оплаты с зафиксированным получателем */}
        <QuickPayToUser recipient={address as `0x${string}`} username={username} />
      </div>
    </main>
  )
}

// Компонент быстрой оплаты конкретному пользователю
function QuickPayToUser({
  recipient,
  username,
}: {
  recipient: `0x${string}`
  username: string
}) {
  return (
    <Card>
      <CardContent className="pt-6">
        <p className="text-sm text-muted-foreground mb-4">
          Перейди на главную и создай запрос — получишь ссылку для <strong>@{username}</strong>
        </p>
        <p className="text-xs font-mono text-muted-foreground break-all">
          Адрес: {recipient}
        </p>
      </CardContent>
    </Card>
  )
}
```

- [ ] **Step 2: Проверить**

```bash
# 1. Зарегистрировать username "testuser" с подключённым кошельком
# 2. Открыть /u/testuser
# 3. Убедиться что адрес отображается корректно
# 4. Открыть /u/nonexistent → должна быть ошибка "не найден"
```

- [ ] **Step 3: Commit**

```bash
git add src/app/u/
git commit -m "feat: /u/[username] profile page via on-chain registry"
```

---

### Task 13: Embed-виджет `/widget/[requestId]`

**Files:**
- Create: `src/components/EmbedCode.tsx`
- Create: `src/app/widget/[requestId]/page.tsx`
- Modify: `next.config.ts`

- [ ] **Step 1: Разрешить iframe embedding в `next.config.ts`**

```ts
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  async headers() {
    return [
      {
        // Только для /widget/* убираем ограничение на framing
        source: '/widget/:path*',
        headers: [
          { key: 'X-Frame-Options', value: 'ALLOWALL' },
          { key: 'Content-Security-Policy', value: "frame-ancestors *" },
        ],
      },
    ]
  },
}

export default nextConfig
```

- [ ] **Step 2: Написать `src/app/widget/[requestId]/page.tsx`**

```tsx
'use client'
import { use } from 'react'
import { usePaymentRequest } from '@/hooks/usePaymentRequest'
import { PayButton } from '@/components/PayButton'
import { WalletConnect } from '@/components/WalletConnect'
import { formatEther } from 'viem'

export default function WidgetPage({
  params,
}: {
  params: Promise<{ requestId: string }>
}) {
  const { requestId } = use(params)
  const id = requestId as `0x${string}`
  const { request, isLoading } = usePaymentRequest(id)

  if (isLoading) {
    return (
      <div className="p-3 text-sm text-gray-500">Загрузка...</div>
    )
  }

  if (!request?.recipient || request.recipient === '0x0000000000000000000000000000000000000000') {
    return (
      <div className="p-3 text-sm text-red-500">Запрос не найден</div>
    )
  }

  const amount: bigint = request.amount ?? 0n
  const amountDisplay = amount > 0n ? `${formatEther(amount)} zkLTC` : 'zkLTC'

  if (request.paid) {
    return (
      <div className="p-3 flex items-center gap-2 text-green-600 text-sm font-medium">
        <span>✓</span>
        <span>Оплачено {amountDisplay}</span>
      </div>
    )
  }

  return (
    // Минималистичный UI для iframe — без шапки сайта
    <div className="p-3 space-y-2 font-sans">
      <p className="text-xs text-gray-500 truncate">{request.label}</p>
      <div className="flex items-center gap-2">
        <span className="font-semibold text-sm">{amountDisplay}</span>
        <WalletConnect />
      </div>
      <PayButton
        requestId={id}
        fixedAmount={amount}
        paid={request.paid}
      />
    </div>
  )
}
```

- [ ] **Step 3: Написать `src/components/EmbedCode.tsx`**

```tsx
'use client'
import { useState } from 'react'
import { Button } from '@/components/ui/button'

interface Props {
  requestId: string
}

export function EmbedCode({ requestId }: Props) {
  const [copied, setCopied] = useState(false)
  const origin = typeof window !== 'undefined' ? window.location.origin : 'https://qrlitvm.app'
  const code = `<iframe\n  src="${origin}/widget/${requestId}"\n  width="300"\n  height="120"\n  frameborder="0"\n  style="border-radius:12px;border:1px solid #e2e8f0"\n></iframe>`

  const copy = async () => {
    await navigator.clipboard.writeText(code)
    setCopied(true)
    setTimeout(() => setCopied(false), 2000)
  }

  return (
    <div className="space-y-3">
      <p className="text-sm text-muted-foreground">
        Вставь код кнопки «Оплатить» на свой сайт:
      </p>
      <pre className="bg-muted rounded-lg p-3 text-xs overflow-x-auto whitespace-pre-wrap break-all">
        {code}
      </pre>
      <Button variant="outline" className="w-full" onClick={copy}>
        {copied ? 'Скопировано!' : 'Копировать код'}
      </Button>
      <p className="text-xs text-muted-foreground">
        Предпросмотр виджета:{' '}
        <a
          href={`/widget/${requestId}`}
          target="_blank"
          className="underline"
        >
          открыть
        </a>
      </p>
    </div>
  )
}
```

- [ ] **Step 4: Проверить embed вручную**

```bash
# 1. Создать запрос → вкладка «Виджет» → скопировать iframe-код
# 2. Вставить <iframe> в любой HTML-файл и открыть через file://
# 3. Убедиться: кнопка «Оплатить» видна и работает
# 4. Проверить: /widget/[id] открывается без шапки сайта
```

- [ ] **Step 5: Commit**

```bash
git add src/components/EmbedCode.tsx src/app/widget/ next.config.ts
git commit -m "feat: embeddable payment widget with iframe support"
```

---

## Chunk 4: Деплой на Vercel

### Task 14: Деплой

- [ ] **Step 1: Убедиться что сборка проходит**

```bash
npm run build
# Ожидаем: ✓ без ошибок TypeScript
```

- [ ] **Step 2: Задеплоить на Vercel**

```bash
npx vercel --prod
# Следовать инструкциям CLI
```

- [ ] **Step 3: Добавить env variables в Vercel**

В Vercel Dashboard → Settings → Environment Variables добавить:
```
NEXT_PUBLIC_LITVM_CHAIN_ID   = <chain id>
NEXT_PUBLIC_CONTRACT_ADDRESS = <адрес контракта>
LITVM_RPC_URL                = <rpc url>
```

- [ ] **Step 4: Проверить production**

```
https://your-app.vercel.app             → главная, генератор
https://your-app.vercel.app/pay/[id]    → страница оплаты
https://your-app.vercel.app/u/alice     → профиль
https://your-app.vercel.app/widget/[id] → embed-виджет
```

- [ ] **Step 5: Финальный commit**

```bash
git add -A
git commit -m "chore: production-ready build"
```

---

## Итого: что получается

| Фича | Реализация | Стоимость |
|------|------------|-----------|
| QR-генератор | qr-code-styling | $0 |
| On-chain запросы | PaymentRequest.sol | gas (testnet) |
| Страница оплаты /pay/[id] | wagmi + viem | $0 |
| Короткие ссылки /u/username | username registry в контракте | gas (testnet) |
| Embed-виджет | /widget/[id] в iframe | $0 |
| Хостинг | Vercel free tier | $0 |
| БД | не нужна (всё on-chain) | $0 |

## После тестнета → mainnet

Когда LitVM выйдет в mainnet: поменять chain ID и RPC URL в `.env`, задеплоить контракт заново. Код менять не нужно.
